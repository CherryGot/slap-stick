/**
 * A wrapper for the styles to save so that they can later be used to restore after the sticker is destroyed.
 */
class StyleStore {

  top?: string;

  position?: string;

}

export default StyleStore;
